# Native Android upgrade inventory

This page lists everything the released React Native (RN) Android app keeps on a phone. A native Kotlin app that installs over it must find this state, keep it, or move it. The list is the input for the later upgrade test harness ("old install → native replacement"). It supports [Native mobile migration §5](native-mobile-migration.md#5-upgrade-compatibility-and-recovery) and [ADR 0029](../adr/0029-native-clients-host-the-typescript-core.md).

- **Read at:** commit `48e56f8ef` (release line 1.3.2, Android `versionCode` 152).
- **Method:** source and build config only. No device, no build. Every claim cites `file:line`. Library facts cite the installed copy under `node_modules/`.
- **UNKNOWN** marks a fact the source cannot prove. Each one says what would settle it.

Terms used below:

- **In-place update:** Android installs a new APK over the old one and keeps the app's private data. This works only when the package name matches, the signing certificate matches (or is a valid rotation of it), and the new `versionCode` is not lower.
- **`<data>`:** the app's private folder, `/data/user/0/<package>/` (also shown as `/data/data/<package>/`). Inside it: `files/` (`Context.filesDir`), `no_backup/`, `cache/`, `databases/`, `shared_prefs/`.
- **AsyncStorage:** the RN key-value store. On Android it is an SQLite file, `<data>/databases/RKStorage`, table `catalystLocalStorage (key, value)`. The app does not turn on the library's newer storage, so this legacy file is the one in use (`node_modules/@react-native-async-storage/async-storage/android/config.gradle:15`, `.../ReactDatabaseSupplier.java:25-32`).
- **SharedPreferences:** Android's XML key-value files under `<data>/shared_prefs/<name>.xml`.
- **SecureStore:** `expo-secure-store`. It encrypts each value with a key held in the Android Keystore (the phone's protected key vault) and saves the encrypted text in a SharedPreferences file.

The store package is `tech.dongdongbh.mindwtr`. Every path below uses it unless a row says otherwise.

## 1. Identity and signing

| Item | Where it lives on device | Owner code (file:line) | Native replacement must | Risk if missed |
| --- | --- | --- | --- | --- |
| Store package name `tech.dongdongbh.mindwtr` | Package manager record | `apps/mobile/app.json:57` | Preserve exactly. Use it for Play, direct APK and FOSS builds. | A different id installs a second app with empty data. |
| FOSS build uses the same package name | Same | `apps/mobile/scripts/android_build.sh:5,43` (no id change for `FOSS_BUILD`); `apps/mobile/scripts/fdroid_strip_deps.js:7-9` (drops only Play modules) | Preserve. F-Droid, IzzyOnDroid and GitHub `-foss.apk` users are all on `tech.dongdongbh.mindwtr`. | Same as above. |
| Dev variant `tech.dongdongbh.mindwtr.dev`, benchmark `tech.dongdongbh.mindwtr.benchmark` | Separate apps | `apps/mobile/app.config.ts:45-46,81-99` | May drop. They never replace the store app. Use `.dev` only for dev-to-dev upgrade rehearsal. | None for users. |
| Current native pilot `tech.dongdongbh.mindwtr.nativeclient.dev` | Separate app, own DB `files/mindwtr-native-dev.db` | `apps/android-native/android/app/build.gradle.kts:12`; `apps/android-native/android/app/src/main/java/tech/dongdongbh/mindwtr/pilot/MainActivity.kt:145` | Re-create for release: switch to the store id and to the RN paths below. | Testing the pilot proves nothing about in-place upgrade. |
| `versionCode` | Package manager record | `apps/mobile/app.json:56` (152); `scripts/bump-version.sh:66` (+1 per release); `scripts/ci/android-version-code-policy.js:36-60` (must exceed Play's highest code) | Continue the same number line, higher than the last RN code on each channel. | Android refuses a lower code. Play refuses a reused code. |
| One code for every artifact | — | `.github/workflows/release-android.yml:278-293` (AAB), `:367-381` (APK); `.github/workflows/release-android-foss.yml:85-91` (FOSS gets the same code); `apps/mobile/plugins/abi-splits.js:14` (no per-ABI offset) | Keep one code per release across channels, or document a new scheme before the first native release. | A channel can end up "newer" than another and block updates. |
| `versionName` | Package manager record | `apps/mobile/app.json:5` (`1.3.2`) | Continue semver. | Cosmetic; update checks compare it (`apps/mobile/modules/play-store-updates/`). |
| Play signing | Play-installed APK signature | `apps/mobile/eas.json:41-45` (`credentialsSource: remote`); `.github/workflows/release-android.yml:293` | Sign the upload with the same EAS upload key, for the same Play app. | Play rejects the upload. |
| Direct GitHub APK `mindwtr-<v>.apk` signing | APK signature | `apps/mobile/eas.json:50-53` (`production-apk` extends `production`, same remote credentials); `.github/workflows/release-android.yml:381` | Sign with the same EAS keystore. | "App not installed" on update; user must uninstall and lose data. |
| GitHub `-foss.apk` signing | APK signature | `.github/workflows/release-android-foss.yml:110` (built unsigned), `:113-188` (signed with secret `ANDROID_RELEASE_KEYSTORE_BASE64`, alias `ANDROID_RELEASE_KEY_ALIAS`) | Sign with the same release keystore. | Same as above. |
| Which channels share a certificate | — | Measured 2026-09-23 with `apksigner verify --print-certs` (see **Measured signing certificates** below) | Sign each channel's native build with that channel's key. Keep F-Droid building from source. | No two of the four user channels can update each other. A user who switches channels must uninstall first. |
| Dev build signing | Dev APK signature | Generated `apps/mobile/android/app/build.gradle:100-115` (debug keystore; file is git-ignored and regenerated by prebuild) | May drop. | None for users. |

### Measured signing certificates

We read the signer of each channel's published build on 2026-09-23. Play App Signing is on. Google holds the key that signs Play installs. Each of the four user channels has its own certificate:

| Channel | Build checked | Signer certificate SHA-256 | Who holds the key |
| --- | --- | --- | --- |
| Google Play install | 1.3.2 `base.apk` pulled from a Play-installed phone (installer `com.android.vending`) | `95ee50118fa90a51b9e8a6d0ee40ead57e212f6817b9487f01b56acfdd3f4ac6` (CN=Android, O=Google Inc.) | Google (Play App Signing) |
| Play upload key, GitHub `mindwtr-<v>.apk` | `mindwtr-1.3.2.aab` from CI run 35764701671; `mindwtr-1.3.1.apk` from the GitHub release | `09902a7f33aec074a1fa5642ef360f349dae6e8b67387d36b523f966a3220ef8` (empty CN, C=US) | Expo EAS remote credentials |
| GitHub `mindwtr-<v>-foss.apk`, IzzyOnDroid | `mindwtr-1.3.1-foss.apk`; IzzyOnDroid `tech.dongdongbh.mindwtr_151.apk` is byte-identical (same SHA-256 `7a34de9c…`) | `1a3e0ab42178306c36534cee2ef3b888756c6e43f0d19de67f7b55246c473df5` (CN=Dongda Li) | Maintainer, GitHub secret `ANDROID_RELEASE_KEYSTORE_BASE64` |
| F-Droid | `tech.dongdongbh.mindwtr_151.apk` from f-droid.org | `8faf7edc3ed8c2f875732480137554a5bbd1e4f80eb93da542737e8a8f186c66` (CN=FDroid) | F-Droid. The fdroiddata recipe has no `Binaries` or `AllowedAPKSigningKeys`, so F-Droid builds from source and signs with its own key. |

What the native release needs, per channel:

- **Play:** upload a native AAB signed with the EAS upload key. Play re-signs it with Google's key. Only Play can produce a Play-signed build, so test this path on a Play testing track.
- **GitHub direct APK:** sign with the EAS keystore. The native app is not an Expo build, so export the keystore from EAS (`eas credentials`) into a CI secret for the Gradle build.
- **GitHub FOSS APK and IzzyOnDroid:** sign with the maintainer release keystore, as today. IzzyOnDroid mirrors the GitHub `-foss.apk` file unchanged.
- **F-Droid:** F-Droid builds from source. Its current recipe uses `subdir: apps/mobile`, npm, and Expo prebuild steps, with `AutoUpdateMode: Version` on `v*` tags. A native release needs a new fdroiddata recipe (Gradle build in `apps/android-native`, Node for the core bundle). Without it, automatic updates build the wrong app or fail. F-Droid's inclusion policy allows prebuilt FLOSS libraries from Maven Central, which covers the QuickJS wrapper and `sqlite-bundled` native libraries.
- **Local harness:** we cannot sign with Google's or F-Droid's key. Local in-place tests can use the EAS key (direct APK path) and the maintainer key (FOSS path). Play and F-Droid need their own channel tests.

### Store rules for the F-Droid and IzzyOnDroid builds

These rules come from the store review threads: [IzzyOnDroid repo#933](https://gitlab.com/IzzyOnDroid/repo/-/work_items/933) (listed 2026-02-17), [F-Droid RFP#3685](https://gitlab.com/fdroid/rfp/-/work_items/3685), and [fdroiddata!35008](https://gitlab.com/fdroid/fdroiddata/-/merge_requests/35008) (merged 2026-05-12). The native FOSS build must keep meeting them.

- **IzzyOnDroid checks reproducibility.** Izzy's builder rebuilds the GitHub `-foss.apk` from the tagged commit and compares the two. The tag must point at the commit that built the APK. The build steps are in `config/izzyonandroid.yml`. Izzy asked us to replace host-dependent values, such as `react_native_dev_server_ip`, with fixed values. Izzy also needed an `apksigner` padding fix.
- **Never replace a published release file.** Izzy said this after we replaced one (2026-02-09). A replaced file breaks their reproducibility check and confuses update clients.
- **Sign with the maintainer release key.** Izzy rejected debug-signed APKs.
- **Size:** Izzy gave a 30 MB per-app limit on 2026-01-01. The 1.3.1 FOSS APK is 35.5 MB. Confirm the current limit with Izzy before a large change.
- **Scanner blockers, both stores:** non-free libraries (Firebase, Google Play services, ML Kit), the Google `DEPENDENCY_INFO_BLOCK` signing block, unneeded dangerous permissions (only `RECORD_AUDIO` and `POST_NOTIFICATIONS` are justified), and `BIND_GET_INSTALL_REFERRER_SERVICE`.
- **Izzy's policy for the FOSS build:** no proprietary AI providers (only a local or custom server), and no update checker unless the user explicitly opts in. The reviewer also checked that the app makes no unexpected network connections.
- **The F-Droid scanner deletes files that reference non-free libraries.** It deleted `expo-application`'s `build.gradle` because of the Play Install Referrer. Autolinking then skipped the module, and the app crashed at launch. The native FOSS flavor (a build variant that leaves out Play-only code) must not reference Play-only libraries anywhere.
- **F-Droid reviewers want build logic upstream.** Keep patch scripts in this repo. The fdroiddata recipe should only call them.
- **F-Droid's automatic update reads `apps/mobile/app.json`.** Its `UpdateCheckData` takes `versionCode` and `version` from that file on `v*` tags. When the native app replaces the RN app, move this setting in the same fdroiddata change as the new recipe.
- **The native app needs FOSS and Play flavors.** The RN app already has them. The FOSS flavor leaves out the Play update module and proprietary AI providers, turns off the update checker, and keeps the diagnostic heartbeat opt-in. This is a parity row.

## 2. Database

| Item | Where it lives on device | Owner code (file:line) | Native replacement must | Risk if missed |
| --- | --- | --- | --- | --- |
| Main database file | `<data>/files/SQLite/mindwtr.db` plus `mindwtr.db-wal` and `mindwtr.db-shm` | `apps/mobile/lib/storage-adapter.ts:55-58,282-303,345-353`; `node_modules/expo-file-system/android/src/main/java/expo/modules/filesystem/FileSystemModule.kt:35-37` (document dir = `filesDir`) | Preserve in place. Open this exact path. Never open a new default path. | The app shows an empty library. A later save or sync can overwrite real data with nothing. |
| Engine that wrote it | op-sqlite 17.1.2, bundled SQLite 3.51.3, FTS5 on, no SQLCipher | `apps/mobile/package.json:27,121-122`; `node_modules/@op-engineering/op-sqlite/android/build.gradle:35,39,139-143`; `.../cpp/sqlite3.h:149` | Open it with androidx bundled SQLite (`apps/android-native/android/app/build.gradle.kts:33`). Check the bundled version is at least as new (U5). | An older engine may fail to read the file or the FTS5 tables. |
| Earlier engine | expo-sqlite used the same `files/SQLite/` folder before op-sqlite | `apps/mobile/lib/storage-adapter.ts:56-57` | Nothing extra: same file. | — |
| Journal and durability | `PRAGMA journal_mode = WAL`, `foreign_keys = ON`, `busy_timeout = 5000`, `temp_store = MEMORY`, set on every open by the core schema. `synchronous` is not set, so it is the build default FULL (op-sqlite `performanceMode` is off) | `packages/core/src/sqlite-schema.ts:8-12`; `apps/mobile/lib/storage-adapter.ts:250-259`; `node_modules/@op-engineering/op-sqlite/android/build.gradle:39,79,138-140` | Keep WAL and FULL. The native bridge already sets both (`apps/android-native/android/app/src/main/java/tech/dongdongbh/mindwtr/pilot/core/SqliteBridge.kt:28-35`). | Weaker durability loses the last writes on power loss. |
| Schema and migration | Tables `tasks`, `projects`, `areas`, `people`, `sections`, `settings`, `saved_filters`, `schema_migrations`, `calendar_sync`; FTS5 `tasks_fts`, `projects_fts` (contentless) with triggers that use JSON1 | `packages/core/src/sqlite-schema.ts:14-162,264-338` | Run the same core `SqliteAdapter.ensureSchema`, not a Kotlin copy. | Two schema writers drift. Missing JSON1 or FTS5 makes every task insert fail (`sqlite-schema.ts:166-175` triggers). |
| Migration mechanism | No `PRAGMA user_version`. Columns are added by idempotent `ensure*Columns`. `schema_migrations` records only version 1 and FTS trigger version 3. `SQLITE_SCHEMA_VERSION = 11` is a log label only | `packages/core/src/sqlite-adapter.ts:263,559-589,651-690`; `packages/core/src/sqlite-schema.ts:1`; `apps/mobile/hooks/root-layout/use-root-layout-startup.ts:170` | Keep migrations additive and in core, so an RN recovery build can still open the file. | A native-only column or table change blocks the RN recovery path (§5 of the roadmap). |
| Recovery checkpoint | Native pilot writes `<db>.prewrite` next to its DB with `VACUUM INTO` before the first write | `apps/android-native/.../pilot/core/SqliteBridge.kt:26,41-61` | Re-create beside `files/SQLite/mindwtr.db`. Keep it local and never back it up. | No safe rollback point if the first native write goes wrong. |
| JSON backup of the whole library | AsyncStorage key `mindwtr-data` (plus `mindwtr-data:startup-backup-version`, `…:startup-backup-updated-at`) | `apps/mobile/lib/storage-adapter.ts:11-19,427-466`; `apps/mobile/lib/widget-data.ts:47` | Migrate: read it once as recovery input. Stop writing it after the switch. | Losing it loses the fallback copy (see next row). |
| "JSON is ahead of SQLite" marker | AsyncStorage key `mindwtr-data:json-ahead-of-sqlite` | `apps/mobile/lib/storage-adapter.ts:14-18,193-229` | Migrate before the first native write: if the key exists, merge `mindwtr-data` into SQLite through the core merge, then clear the key. | Writes that SQLite refused live only in the JSON backup. Ignoring the marker drops them silently (#964). |
| One-time reconcile flag and legacy keys | `mindwtr-data:sqlite-json-reconcile-v1`; legacy `focus-gtd-data`, `gtd-todo-data`, `gtd-data` | `apps/mobile/lib/storage-adapter.ts:20,370-381,775-800` | Migrate or treat as done. Only a very old install still has legacy keys. | A user jumping from a very old RN version loses data the RN app would have migrated. |
| JSON backup size limit | Android rows over ~2 MB cannot be read back through a `CursorWindow` | `apps/mobile/lib/storage-adapter.ts:396-405` | Read `RKStorage` with a driver that has no row cap, or expect the backup to be absent. | A read error must not be taken as "no data". |

## 3. Files outside the database

| Item | Where it lives on device | Owner code (file:line) | Native replacement must | Risk if missed |
| --- | --- | --- | --- | --- |
| Attachment files | `<data>/files/attachments/…`. Task and project rows store absolute `file://` URIs to them | `packages/core/src/attachment-paths.ts:6`; `apps/mobile/lib/attachment-sync-utils.ts:395-399`; `packages/core/src/types.ts:153-202` (`Attachment`) | Preserve in place. Keep the same folder so stored URIs stay valid. | Attachments show as missing and may be downloaded again or lost. |
| Attachment installer journals | In `files/attachments/`: `.mindwtr-attachment-installer.lock`, `.mindwtr-install-<digest>.journal/.candidate/.quarantine`, `.mindwtr-preserved-*`, `*.retiring` | `apps/mobile/modules/attachment-file-installer/android/src/main/java/tech/dongdongbh/mindwtr/attachmentfileinstaller/AttachmentFileInstallerCore.kt:7-10,137-139,904,941-943`; `.../AttachmentFileInstallerModule.kt:214,538` (hard link with `Os.link`, fallback on EXDEV/EPERM) | Migrate: finish or roll back each journal with the same rules, then remove artifacts. Do not delete them blindly. | A crash mid-install leaves a half-written or quarantined attachment. |
| File-mtime fields | `contentMtimeMs`, `contentSize` in attachment JSON | `packages/core/src/types.ts:177-187` | Preserve files in place (no copy). | A copy changes mtime and forces a hash re-check of every attachment. Not data loss. |
| Pending-captures queue | `<data>/files/pending-captures/<uuid>.json` (and `<uuid>.tmp` partials). Kinds: capture/text, `complete`, `audio`, `defer` | `apps/mobile/lib/pending-captures.ts:24,28-70`; `apps/mobile/modules/android-widget/android/src/main/java/tech/dongdongbh/mindwtr/androidwidget/PendingCaptureWriter.kt:14-26,165-171` | Migrate: drain every item into the store on first native start, through core. | Captures and widget check-offs made before the update are lost. |
| Quick-capture audio | `<data>/files/quick-capture-audio/<uuid>.wav` (staged as `.<uuid>.wav.tmp`) | `PendingCaptureWriter.kt:23,156-158`; `apps/mobile/lib/pending-captures.ts:258` | Migrate with the queue item that names it. | Voice captures are lost. |
| Widget check-off pending state | `<data>/files/mindwtr-widget-checkoff-pending.json` (AtomicFile) | `apps/mobile/modules/android-widget/android/src/main/java/tech/dongdongbh/mindwtr/androidwidget/PendingCheckoffStore.kt:15,64` | Migrate: commit or cancel open check-offs. | A check-off tapped just before the update is lost or repeated. |
| Capture-intent token | `<data>/no_backup/android-capture-intent.json` (AtomicFile, may have `.bak`/`.new`) | `apps/mobile/modules/android-widget/android/src/main/java/tech/dongdongbh/mindwtr/androidwidget/CaptureIntentConfigStore.kt:42-43,86-92,149` | Preserve in place. It is a device secret (§5). | Automation apps that send `tech.dongdongbh.mindwtr.action.CAPTURE` stop working. |
| File Sync folder locks | `<data>/files/file-sync-root-authorities/<sha256>.lock`; `.mindwtr.lock` inside the chosen sync folder | `apps/mobile/modules/sync-file-lock/android/src/main/java/tech/dongdongbh/mindwtr/syncfilelock/SyncFileLockModule.kt:23,385-391,411` | Re-create with the same names and locking rule. | Two writers can publish to the sync folder at once. |
| Recovery snapshots | `<data>/files/snapshots/data.<iso>.snapshot.json` | `apps/mobile/lib/recovery-snapshot.ts:21,33-46` | Preserve. Show them in the restore UI. | Users lose older restore points. |
| App log and crash file | `<data>/files/logs/mindwtr.log`; `<data>/files/logs/mindwtr-fatal-js-crash.json` | `apps/mobile/lib/app-log.ts:108-112`; `apps/mobile/lib/mobile-crash-capture.ts:5-6` | May drop the crash file. Keep writing the log at the same path so Settings › Diagnostics still shares it. | Testers cannot send the log that proves a fix. |
| Whisper speech models | `<data>/files/whisper-models/<file>`, or `<data>/cache/whisper-models/` when documents are not writable. The full path is saved in settings `offlineModelPath` | `apps/mobile/lib/whisper-model-store.ts:36,290-297`; `packages/core/src/types.ts:498` | Preserve in place, or re-download and rewrite `offlineModelPath`. | Offline transcription breaks. The model is large to fetch again. |
| Temporary files | `<data>/cache/` (audio recordings, share cards, export files, attachment downloads) | `apps/mobile/components/use-quick-capture-audio.ts:494-501`; `apps/mobile/lib/share-card-export.ts:155`; `apps/mobile/lib/data-transfer.ts:530`; `apps/mobile/lib/attachment-sync-utils.ts:596` | May drop, except a Whisper model in cache (row above). | None beyond that. |
| Benchmark profiles | App external files `capture-profiles/` (benchmark package only) | `apps/mobile/modules/startup-metrics/android/src/main/java/tech/dongdongbh/mindwtr/startupmetrics/StartupMetricsModule.kt:20-30` | May drop. | None. |
| File Sync folder grant | Android keeps a persisted SAF grant for the chosen folder. SAF (Storage Access Framework) is Android's system folder picker. The URI is in AsyncStorage `@mindwtr_sync_path` | `apps/mobile/lib/storage-file.ts:559-563`; `node_modules/expo-file-system/android/src/main/java/expo/modules/filesystem/legacy/FileSystemLegacyModule.kt:724`; `apps/mobile/lib/sync-constants.ts:1` | Preserve: the grant belongs to the package and survives an in-place update. Read the URI from the old key. | File Sync fails and the user must pick the folder again. |

## 4. Settings and preferences

| Item | Where it lives on device | Owner code (file:line) | Native replacement must | Risk if missed |
| --- | --- | --- | --- | --- |
| All app settings (synced and device-local) | One JSON row in DB table `settings` (`id = 1`) | `packages/core/src/sqlite-schema.ts:132-135` | Preserve in place (it is in the DB). | — |
| Which setting groups sync | `appearance`, `language`, `gtd`, `externalCalendars`, `ai`, `savedFilters` | `packages/core/src/sync-merge-settings.ts:212,845` | Keep using core merge. | A Kotlin copy of the rules would leak local fields to other devices. |
| Device-local fields inside that row | `deviceId`, `lastSyncAt`, `lastSyncStatus`, `lastSyncError`, `pendingRemoteWriteAt`, `pendingRemoteWriteRetryAt`, `pendingRemoteWriteAttempts`, `lastSyncStats`, `lastSyncHistory`, device-local AI fields, `offlineModelPath` | `packages/core/src/types.ts:577-590,498`; `packages/core/src/sync-merge-settings.ts:181` | Preserve. Keep the same `deviceId`. | A new `deviceId` changes `revBy` and conflict tie-breaks. Losing `pendingRemoteWriteAt` drops the "upload owed" mark. |
| Sync configuration (not secret) | AsyncStorage: `@mindwtr_sync_backend`, `@mindwtr_sync_path`, `@mindwtr_webdav_url`, `@mindwtr_webdav_username`, `@mindwtr_webdav_allow_insecure_http`, `@mindwtr_webdav_allow_weak_fingerprint`, `@mindwtr_cloud_url`, `@mindwtr_cloud_provider`, `@mindwtr_cloud_allow_insecure_http` | `apps/mobile/lib/sync-constants.ts:1-12` | Migrate: read these keys from `RKStorage` on first start. | Sync is silently off after the update. |
| Theme and language | AsyncStorage `@mindwtr_theme`, `@mindwtr_theme_style`, `mindwtr-language` | `apps/mobile/contexts/theme-context.tsx:26-27`; `packages/core/src/i18n/i18n-constants.ts:4` | Migrate. | The app changes look and language after the update. |
| View and session state | AsyncStorage `mindwtr:view:focus:v1`, `mindwtr:view:projects:v1`, `mindwtr:view:archived:v1`, `mindwtr:view:done:v1`, `mindwtr:view:taskOpenMode:v1`, `mindwtr:view:inboxProcessingMode:v1`, `mindwtr:view:project-detail:show-completed:v1`, `mindwtr:session:lastRoute`, `mindwtr:dailyReview:currentStep`, `mindwtr:weeklyReview:currentStep`, `lastWeeklyReview`, `mindwtr:settings:manage:openSections` | `apps/mobile/app/(drawer)/(tabs)/focus.tsx:112`; `apps/mobile/lib/view-state/*.ts` (one key each); `apps/mobile/lib/session-restore.ts:8`; `apps/mobile/components/daily-review-modal.tsx:50`; `apps/mobile/components/review/useReviewModalController.ts:77,303`; `apps/mobile/components/settings/manage-settings-screen.tsx:31`; `apps/mobile/components/projects-screen/ProjectDetailModal.tsx:62` | Migrate the ones the native UI has. The rest may drop. | Lost sort and filter choices. Annoying, not data loss. |
| Feature and prompt state | AsyncStorage `mindwtr:mobile:first-run-onboarding:v1`, prefix `mindwtr:mobile:onboarding-hint:v1:`, `mindwtr:local-user-prompts:v1`, `mindwtr-analytics-distinct-id`, `mindwtr-update-available`, `mindwtr-update-last-check`, `mindwtr-update-latest`, `mindwtr-ai-provider-consent-v1`, `mindwtr:quickCapture:addAnother`, `mindwtr:persistentCaptureNotification`, `mindwtr:appSearchIndexingEnabled`, `@mindwtr_pomodoro_state`, `@mindwtr_pomodoro_collapsed` | `apps/mobile/lib/mobile-onboarding-events.ts:5`; `apps/mobile/lib/onboarding-hints.ts:4`; `apps/mobile/lib/user-prompt-state.ts:8`; `apps/mobile/lib/analytics-heartbeat.ts:12`; `apps/mobile/components/settings/settings.constants.ts:248-252`; `apps/mobile/lib/quick-capture-preferences.ts:6`; `apps/mobile/lib/persistent-capture-notification.ts:11`; `apps/mobile/lib/app-search-preference.ts:8`; `apps/mobile/lib/pomodoro-session.ts:15-17` | Migrate AI consent, analytics id, persistent-capture toggle and app-search toggle. The rest may drop. | Onboarding shows again. A new analytics id double-counts a user. AI consent is asked again. |
| Calendar state | AsyncStorage `mindwtr-external-calendars`, `mindwtr-system-calendar-settings`, `mindwtr:calendar-push-sync:enabled`, `…:calendar-id`, `…:target-calendar-id`, `…:color` | `apps/mobile/lib/external-calendar.ts:21-22`; `apps/mobile/lib/calendar-push-sync.ts:44-51` | Migrate. Reuse the saved calendar id. | A second "Mindwtr" calendar and duplicate events appear. |
| Sandbox boot request | AsyncStorage `@mindwtr_sandbox_boot_request_v1` | `apps/mobile/lib/sandbox-workspace.ts:28` | May drop (clear it). The sandbox itself is memory-only (`apps/mobile/lib/workspace-session-storage.ts:13-17`). | None. |
| iOS-only keys | `@mindwtr_sync_path_bookmark`, `@mindwtr_cloudkit_*`, `mindwtr-apple-reminders-import-v1`, `mindwtr-ios-*`, `mindwtr:appleClarificationBackend:v1` | `apps/mobile/lib/sync-constants.ts:2,21-23`; `apps/mobile/lib/apple-reminders-import.ts:8`; `apps/mobile/lib/widget-data.ts:50,60` | May drop on Android. | None. |
| Widget payload | SharedPreferences `mindwtr_widget`, key `payload` | `apps/mobile/modules/android-widget/android/src/main/java/tech/dongdongbh/mindwtr/androidwidget/WidgetPayload.kt:519-533` | Preserve the format, or rebuild it on first start. | Widgets stay blank until the app runs. |
| Widget list per placed widget | SharedPreferences `mindwtr_widget_lists`, key = appWidgetId | `.../androidwidget/WidgetListStore.kt:7-21` | Preserve in place. | Every placed widget falls back to its default list. |
| Widget check-off stores | SharedPreferences `mindwtr_widget_checkoff` (legacy, migrated once), `mindwtr_widget_checkoff_committed`, `mindwtr_widget_checkoff_diagnostics` | `.../androidwidget/CheckoffStore.kt:43-45`; `.../androidwidget/PendingCheckoffStore.kt:17-33` | Preserve, or finish pending items and clear. | Hidden rows stay hidden, or a check-off repeats. |
| Widget render fingerprint | AsyncStorage `mindwtr-widget-render-fingerprint` | `apps/mobile/lib/widget-service.ts:379` | May drop (forces one re-render). | None. |
| Persistent capture notification mirror | SharedPreferences `mindwtr-persistent-capture` (`enabled`, `title`, `text`, `channelName`) | `apps/mobile/modules/notification-open-intents/android/src/main/java/tech/dongdongbh/mindwtr/notificationopenintents/PersistentCaptureNotifier.kt:27-36` | Preserve, or rebuild from the AsyncStorage toggle. | The pinned capture notification disappears. |

## 5. Credentials and secrets

Names only. No value was read or copied.

| Item | Where it lives on device | Owner code (file:line) | Native replacement must | Risk if missed |
| --- | --- | --- | --- | --- |
| SecureStore file and item names | SharedPreferences `SecureStore`, entry key `key_v1-<name>` | `node_modules/expo-secure-store/android/src/main/java/expo/modules/securestore/SecureStoreModule.kt:366-387` | Preserve in place. Read with a compatible decoder, or migrate once to a new store and verify before deleting. | All sync credentials look missing. |
| Keystore key | Android Keystore alias `AES/GCM/NoPadding:key_v1:keystoreUnauthenticated` (`requireAuthentication` is never set) | `.../securestore/encryptors/AESEncryptor.kt:34-51,137`; `apps/mobile/lib/secure-config.ts:30-32` | Preserve. Keystore keys belong to the app's user id and survive an in-place update. Never delete the alias. | Deleting it makes every stored secret unreadable. |
| Entry encoding | JSON `{ ct, iv, tlen, scheme: "aes", keystoreAlias: "key_v1", requireAuthentication: false, usesKeystoreSuffix: true }`. `ct` and `iv` are Base64 (no line wrap). AES-GCM | `.../encryptors/AESEncryptor.kt:98-107,113-130`; `.../SecureStoreModule.kt:205,227-233` | Decode exactly this format. | Wrong decoding reads garbage or fails. |
| WebDAV password | SecureStore `mindwtr_webdav_password` (legacy plain AsyncStorage `@mindwtr_webdav_password`) | `apps/mobile/lib/secure-config.ts:17-26`; `apps/mobile/lib/sync-constants.ts:6` | Preserve. Also migrate the legacy plain key if it still exists, then delete it. | WebDAV sync stops. A plain-text copy lingers. |
| Self-hosted cloud token | SecureStore `mindwtr_cloud_token` (legacy `@mindwtr_cloud_token`) | `apps/mobile/lib/secure-config.ts:17-26`; `apps/mobile/lib/sync-constants.ts:10` | Same as above. | Cloud sync stops. |
| Sync encryption key | SecureStore `mindwtr_sync_encryption_key_v1`: the derived 32-byte key, Base64. The passphrase is never stored | `apps/mobile/lib/sync-constants.ts:18-20`; `apps/mobile/lib/sync-encryption-state.ts:203-214` | Preserve. | Encrypted sync stops until the user types the passphrase again. |
| Sync encryption state (not secret) | AsyncStorage `@mindwtr_sync_encryption_state_v1` (state, salt, KDF parameters, unfinished transition) | `apps/mobile/lib/sync-constants.ts:14-17`; `apps/mobile/lib/sync-encryption-state.ts:135-182,261` | Migrate before the first sync. | The app may treat an encrypted remote as plain, or lose an unfinished enable/disable. |
| Dropbox tokens | SecureStore `mindwtr_dropbox_tokens` (legacy `@mindwtr_dropbox_tokens`) | `apps/mobile/lib/dropbox-auth.ts:13-14,40-90` | Preserve. Build with the same Dropbox app key and redirect `mindwtr://redirect`. | Token refresh fails; the user must sign in again. |
| Dropbox app key | Baked in at build time (`DROPBOX_APP_KEY`) | `apps/mobile/app.config.ts:33,116`; `.github/workflows/release-android.yml:241`; `.github/workflows/release-android-foss.yml:41`; `apps/mobile/lib/dropbox-auth-callback.ts:1` | Preserve the same key per channel. | Same as above. |
| AI provider keys | SecureStore `mindwtr-ai-key_gemini`, `mindwtr-ai-key_openai`, `mindwtr-ai-key_anthropic` (legacy AsyncStorage `mindwtr-ai-key:<provider>`) | `packages/core/src/ai-config.ts:5,16-18`; `packages/core/src/ai/types.ts:3`; `apps/mobile/lib/ai-config.ts:15-17,19-72` | Preserve. Migrate legacy plain keys. | AI features stop; the user must paste keys again. |
| Capture-intent token | `<data>/no_backup/android-capture-intent.json` | `CaptureIntentConfigStore.kt:42-43,92,149` | Preserve in place. | Automations stop until the token is re-copied. |
| App lock | No stored secret. Uses the system biometric/passcode prompt; the on/off setting is in DB settings | `apps/mobile/lib/mobile-app-lock.ts:1-60`; `packages/core/src/types.ts:513-515` (`security.mobileAppLockEnabled`); `apps/mobile/app/_layout.tsx:1211` | Re-create the gate with the system prompt. | App opens unlocked after the update. |
| Old backups | Before v1.2.5 (`7c27f03ef`), backup rules included every SharedPreferences file, `SecureStore` too | `apps/mobile/plugins/android-manifest-fixes.js:111-117` | Treat "entry present, key missing" as "credential missing", not as a crash. | A phone restored from an old backup crashes or wedges sync. |
| Desktop credential-state rename (#1007) | Desktop only (`dropbox-credential-state.json`). Mobile has no such file | Grep of `apps/mobile` finds no `credential-state` | No action on Android. | — |

## 6. Background and OS-registered state

Class names below are fixed. Android launchers, widget hosts, tiles and alarms point at them by name.

| Item | Where it lives on device | Owner code (file:line) | Native replacement must | Risk if missed |
| --- | --- | --- | --- | --- |
| Launcher activity | `tech.dongdongbh.mindwtr.MainActivity` (`.MainActivity` under the package namespace), `singleTask` | `apps/mobile/plugins/android-manifest-fixes.js:6,228-258`; generated manifest `apps/mobile/android/app/src/main/AndroidManifest.xml:37-74` | Keep this class name, or add an `activity-alias` with it. | Home-screen icons and pinned shortcuts vanish. |
| Deep links | Scheme `mindwtr` (and `exp+mindwtr`). Routes: `capture`, `quick-capture`, `add-task`, `new-task`, `focus`, `today`, `next`, `waiting`, `someday`, `projects`, `review`, `calendar`, `inbox`, `global-search`, `open-feature`, `open?task=|project=|area=`, `redirect` (Dropbox sign-in) | `apps/mobile/app.json:8`; `apps/mobile/lib/capture-deeplink.ts:18-20,159-187`; `apps/mobile/lib/app-search-projection.ts:60-82`; `apps/mobile/lib/dropbox-auth-callback.ts:1` | Preserve every route. | Shortcuts, search results, automations and Dropbox sign-in open nothing. |
| Share and assistant intents | `SEND` / `SEND_MULTIPLE` (text, image, audio, video, application), `com.google.android.gms.actions.CREATE_NOTE` | `apps/mobile/app.json:123-147`; `apps/mobile/plugins/android-app-shortcuts.js:10`; generated manifest `:49-72` | Preserve. | "Share to Mindwtr" and voice-assistant notes stop. |
| App shortcuts | Static ids `capture`, `inbox`, `focus`, `waiting`, `someday`, `projects`, `review`, `calendar`; capability ids `add_task_inbox`, `open_focus`; resource `@xml/mindwtr_shortcuts` | `apps/mobile/plugins/android-app-shortcuts.js:6,24-60,64-140` | Keep the same ids and targets. | Pinned shortcuts are disabled by the launcher. |
| Home-screen widgets | Providers `tech.dongdongbh.mindwtr.androidwidget.TasksWidgetProvider`, `.CompactWidgetProvider`, `.QuickCaptureWidgetProvider`, and legacy `tech.dongdongbh.mindwtr.widget.TasksWidget` (1.2.7 widgets) | `apps/mobile/plugins/android-widget.js:11-18,45-111,327-331` | Keep all four class names and their info XML. | Placed widgets turn into "Can't load widget" or disappear. |
| Widget activities, service, receiver | `QuickCaptureActivity`, `WidgetConfigureActivity` (exported), `WidgetTapActivity`, `TaskPeekActivity`, `TasksWidgetService`, `CaptureIntentReceiver` (exported, action `tech.dongdongbh.mindwtr.action.CAPTURE`, extras `text`, `token`), `CaptureSyncHeadlessService` | `apps/mobile/plugins/android-widget.js:12-18,211-308`; `.../androidwidget/CaptureIntentProcessor.kt:23-25`; `apps/mobile/modules/android-widget/android/src/main/AndroidManifest.xml:8-10` | Keep exported names and actions. Internal ones may change if every PendingIntent is rebuilt. | External automation breaks. Old widget PendingIntents fail. |
| Widget request codes and sweep alarm | Request codes 4612, 4613, 4614; bases `1 shl 20`, `2 shl 20`, `3 shl 20`; action `tech.dongdongbh.mindwtr.androidwidget.CHECKOFF_SWEEP` sent to `TasksWidgetProvider` | `.../androidwidget/WidgetRenderer.kt:40-48`; `.../androidwidget/CheckoffStore.kt:47,50,303-307` | Re-create. Handle the sweep action on first start. | A pending check-off is never committed. |
| Quick Settings tile | `tech.dongdongbh.mindwtr.quicksettings.CaptureTileService` | `apps/mobile/plugins/android-quick-settings-tile.js:8,35` | Keep the class name. | The user's added tile vanishes. |
| Context automation | Exported receiver `tech.dongdongbh.mindwtr.contextautomation.ContextAutomationReceiver`, actions `tech.dongdongbh.mindwtr.action.ACTIVATE_CONTEXT` / `DEACTIVATE_CONTEXT` (with and without `mindwtr:` data); headless task `MindwtrContextAutomation` | `apps/mobile/plugins/android-manifest-fixes.js:10-15,86-109`; `apps/mobile/lib/context-automation-headless-task.ts:9,80` | Keep receiver name and actions. | Tasker-style automations stop. |
| Reminder channel | Channel id `mindwtr_reminders_v2`, name "Mindwtr reminders" | `apps/mobile/lib/notification-service-local.ts:94-95`; `apps/mobile/modules/notification-open-intents/.../NotificationOpenIntentsModule.kt:34-63` | Keep the id. Users' sound and importance choices are stored per channel id. | Custom reminder sound and priority are lost. |
| Persistent capture channel | Channel `mindwtr-persistent-capture`, notification id 41120, request codes 41120/41121 | `PersistentCaptureNotifier.kt:13-21` | Keep the id. | Same as above. |
| Scheduled reminders | Alarm library database `<data>/databases/rnandb`, table `alarmtbl (id, gson_data, active)`; AlarmManager PendingIntents target `com.emekalites.react.alarm.notification.AlarmReceiver` with request code = row id. Map of task → alarm in AsyncStorage `mindwtr:local:alarms:v1`, `mindwtr:local:pomodoro-alarm:v1` | `node_modules/react-native-alarm-notification/android/src/main/java/com/emekalites/react/alarm/notification/AlarmDatabase.java:17-28`; `apps/mobile/lib/notification-service-local.ts:92-93,560-600`; `apps/mobile/plugins/patch-alarm-notification-gradle.js:54-139` | Migrate: cancel every old alarm by its row id, then re-create reminders from the DB. | Old alarms fire into a missing class (silent), or both old and new fire (duplicates). |
| Boot receiver | `com.emekalites.react.alarm.notification.AlarmBootReceiver` (exported, starts disabled; enabled when alarms exist) | `apps/mobile/plugins/patch-alarm-notification-gradle.js:1602-1643` | Re-create an equivalent boot receiver. | Reminders stop after a reboot. |
| Background sync job | WorkManager unique work `EXPO_BACKGROUND_WORKER` (class `expo.modules.backgroundtask.BackgroundTaskWork`); task `mindwtr-background-sync` registered in SharedPreferences `TaskManagerModule` | `node_modules/expo-background-task/android/src/main/java/expo/modules/backgroundtask/BackgroundTaskScheduler.kt:32,129-130`; `node_modules/expo-task-manager/android/src/main/java/expo/modules/taskManager/TaskService.java:51`; `apps/mobile/lib/background-sync-task.ts:19,380,505-513` | Migrate: cancel the old unique work on first start, then schedule the native job. | The old job runs a missing class every period, or two sync jobs run. |
| Headless capture sync | Headless task `MindwtrCaptureSync` started by `CaptureSyncHeadlessService` | `apps/mobile/lib/background-sync-task.ts:22,392`; `.../androidwidget/CaptureSyncHeadlessService.kt:11` | Re-create. | Widget captures wait until the app opens. |
| System search index | Android AppSearch (system storage), database `mindwtr_search`, namespace `mindwtr`, schema `MindwtrItem`, deep link `mindwtr://open?…` | `apps/mobile/modules/app-search/android/src/main/java/tech/dongdongbh/mindwtr/appsearch/MindwtrAppSearchIndex.kt:16-25,125` | Preserve the deep link format, or clear and rebuild the index. | Search results open nothing, or show deleted tasks. |
| Device calendar | A local calendar titled "Mindwtr" (name `mindwtr`) in the system Calendar Provider; task → event map in DB table `calendar_sync` | `apps/mobile/lib/calendar-push-sync.ts:50-51`; `packages/core/src/sqlite-schema.ts:155-162` | Preserve. Reuse the saved calendar id (§4). | Duplicate calendar and events. |
| Runtime permissions | Notifications, exact alarms, calendar, microphone, biometrics | `apps/mobile/app.json:58-64`; generated manifest `:4-16` | Declare the same permissions. Grants survive an in-place update only when still declared. | Reminders, calendar sync or voice capture stop and ask again. |
| Cleartext and network security config | `usesCleartextTraffic="true"`; `@xml/network_security_config` | `apps/mobile/plugins/android-manifest-fixes.js:203-205`; `apps/mobile/plugins/android-network-security-config.js` | Keep equivalent rules. | WebDAV on a local-network HTTP server stops working. |

## 7. Sync state

| Item | Where it lives on device | Owner code (file:line) | Native replacement must | Risk if missed |
| --- | --- | --- | --- | --- |
| Per-row revisions and tombstones | DB columns `rev`, `revBy`, `updatedAt`, `deletedAt`, `purgedAt` on each entity table | `packages/core/src/sqlite-schema.ts:54-59,79-84` | Preserve in place. | Deletes come back from other devices. Edits lose ties. |
| Unsynced-write marker and last sync | DB settings row (`pendingRemoteWriteAt` and friends, `lastSync*`) | `packages/core/src/types.ts:577-584` | Preserve. | Local edits are never pushed. |
| Attachment upload owed | Attachment field `pendingContentUpload`, `localStatus` (local only, stripped from remote) | `packages/core/src/types.ts:188-202`; `packages/core/src/sync.ts:988-993` | Preserve. | Edited attachment bytes never upload. |
| Remote attachment deletes owed | DB settings `attachments.pendingRemoteDeletes` | `packages/core/src/types.ts:417-420` | Preserve. | Orphan files stay on the server. |
| Fast-check cache | AsyncStorage `@mindwtr_fast_sync_state_v1` | `apps/mobile/lib/sync-service.ts:99,173-186` | May drop (the next sync does a full check). | One slower sync. |
| Local sync status | AsyncStorage `@mindwtr_local_sync_status_v1` | `apps/mobile/lib/sync-service.ts:100,208-226` | Migrate or drop. | Status line shows "never synced" once. |
| Dropbox cursor | AsyncStorage `@mindwtr_dropbox_last_rev` | `apps/mobile/lib/sync-constants.ts:13`; `apps/mobile/lib/sync-service.ts:906,1016-1019` | Migrate. | Extra full download; conflict check less precise once. |
| WebDAV capability proof | AsyncStorage `@mindwtr_webdav_capability_proof_v1`, `@mindwtr_webdav_legacy_proof_v1` | `apps/mobile/lib/webdav-capability-proof.ts:10-11` | Migrate or re-prove. | Sync may refuse to run until the server is proven again. |
| Background failure back-off | AsyncStorage `@mindwtr_background_sync_failure_state_v1`, `@mindwtr_background_sync_last_registered_interval` | `apps/mobile/lib/sync-constants.ts:24-32` | May drop. | A failing setup retries a few extra times. |
| Attachment reconcile and File Sync reservations | AsyncStorage `@mindwtr_attachment_presence_reconcile_v1`, `@mindwtr/file-sync-publication-reservations-v1`; staged names `.mindwtr-generation-stage-*`, `.mindwtr-install-*` in the sync folder | `apps/mobile/lib/attachment-sync-utils.ts:873`; `apps/mobile/lib/attachment-file-installer.ts:105-107` | Migrate reservations and clean staged files with the same rules. | Stray staged files in the user's sync folder. |
| Remote fence / lease | Stored on the remote, owner `mindwtr-mobile` | `packages/core/src/sync-remote-fence.ts:10,181`; `apps/mobile/lib/sync-encryption-service.ts:138` | Keep the owner id, or wait out the lease. | A fence left by the RN app blocks the first native sync until it expires. |
| Pending captures and widget check-offs | Files in §3 | §3 | Drain before the first sync. | Captured work is lost or synced late. |

## 8. Backup

| Item | Where it lives on device | Owner code (file:line) | Native replacement must | Risk if missed |
| --- | --- | --- | --- | --- |
| `android:allowBackup` | `true` (Expo default; the plugin leaves it alone) | `apps/mobile/plugins/android-manifest-fixes.js:117`; generated manifest `:26` | Either keep `true` with the rules below, or set `false` (the pilot does: `apps/android-native/android/app/src/main/AndroidManifest.xml:7`). Record the choice. | A wide rule set would upload secrets and the DB to Google backup. |
| `fullBackupContent` (Android 11 and lower) | `@xml/mindwtr_backup_rules`: includes only `sharedpref/mindwtr_backup_none` (a file that never exists), so nothing is backed up | `apps/mobile/plugins/android-manifest-fixes.js:118-145` | Preserve "back up nothing". | — |
| `dataExtractionRules` (Android 12+) | `@xml/mindwtr_data_extraction_rules`: same single include for `cloud-backup` and `device-transfer` | `apps/mobile/plugins/android-manifest-fixes.js:130-140` | Preserve. | Device-to-device transfer could copy a Keystore-bound SecureStore file that cannot decrypt on the new phone. |
| Rules history | In force since v1.2.5-rc.1 (`7c27f03ef`); before that the SecureStore default included all SharedPreferences | `apps/mobile/plugins/android-manifest-fixes.js:111-117` | Handle old restored installs (§5 "Old backups"). | — |

## (a) What the upgrade harness must seed and assert

Priority order. "Seed" means: create this state with a real signed RN release build before installing the native build over it.

1. **Signing and version.** Locally, install the latest RN GitHub APK and `-foss.apk`, then install the native build signed with the same key over each. Assert the update succeeds without uninstall, and `versionCode` is higher. Test Play-signed and F-Droid-signed updates on their own channels, because only Google and F-Droid hold those keys.
2. **Database in place.** Seed tasks, projects, areas, people, sections, saved filters, tombstones, calendar rows and FTS search hits. Kill the RN app mid-write so the `-wal` file holds committed rows. Assert the native app opens `files/SQLite/mindwtr.db`, shows every row, search works, `deviceId` is unchanged, and `PRAGMA journal_mode` is `wal`.
3. **JSON-ahead recovery.** Seed `mindwtr-data` plus `mindwtr-data:json-ahead-of-sqlite` with edits not in SQLite. Assert the edits appear after upgrade and the marker is cleared.
4. **Unsynced work.** Seed `pendingRemoteWriteAt`, an attachment with `pendingContentUpload`, files in `pending-captures/` (all four kinds) and `quick-capture-audio/`, and an open widget check-off. Assert every item lands once, and the next sync pushes it.
5. **Credentials.** Configure WebDAV, self-hosted cloud, Dropbox and sync encryption, plus one AI key. Assert the native app syncs without asking for anything. Also seed one legacy plain AsyncStorage secret. Assert it moves to secure storage and the plain copy is gone. Never log values.
6. **Attachments.** Seed attachments, including one interrupted install with a `.mindwtr-install-*` journal. Assert files open, stored URIs resolve, and the journal is resolved, not deleted.
7. **OS components.** Place all four widget kinds (including a legacy `widget.TasksWidget` from a 1.2.7 install), pin two shortcuts, add the Quick Settings tile, enable the persistent capture notification, send a capture intent with the saved token. Assert all still work after the update.
8. **Reminders and jobs.** Seed future reminders and a Pomodoro alarm. Assert each fires exactly once after the update and after a reboot, and that `EXPO_BACKGROUND_WORKER` is gone.
9. **File Sync grant.** Seed File Sync on a SAF folder. Assert sync works with no folder re-pick.
10. **Device-local settings.** Seed theme, language, view state, calendar push target and AI consent. Assert the ones the native UI supports survive.
11. **Failure cases.** Unreadable DB page, oversized `mindwtr-data`, missing Keystore key with a present SecureStore entry, denied permissions, and a kill during the first native start. Assert the app never saves an empty snapshot over real data (roadmap §5).
12. **Recovery.** Install a newer RN recovery build over the native build after native edits. Assert it opens the DB and keeps those edits.

## (b) Open UNKNOWNs

| Id | Unknown | What would settle it |
| --- | --- | --- |
| U1 | RESOLVED 2026-09-23: Play App Signing is on. Play installs carry Google's key `95ee5011…`; the upload key is the EAS key `09902a7f…`. | See **Measured signing certificates** in section 1. |
| U2 | RESOLVED 2026-09-23: No. The FOSS key `1a3e0ab4…` differs from the EAS key `09902a7f…`. | See **Measured signing certificates** in section 1. |
| U3 | RESOLVED 2026-09-23: F-Droid builds from source and signs with its own key `8faf7edc…`. IzzyOnDroid ships the GitHub `-foss.apk` unchanged, signed with the maintainer key. | See **Measured signing certificates** in section 1. |
| U4 | Full list of SharedPreferences files and AsyncStorage keys on a real install. This page lists keys found by reading constants; library-owned or dynamic keys may be missing. | Dump `shared_prefs/` and `SELECT key FROM catalystLocalStorage` from a disposable old install. |
| U5 | SQLite version inside `androidx.sqlite:sqlite-bundled:2.7.1`, and that it has FTS5 and JSON1 on (the pilot's schema creation suggests yes). | Run `select sqlite_version()` and a `tasks_fts` query through `SqliteBridge` on the old file. |
| U6 | Whether Android keeps old AlarmManager entries and WorkManager rows through the update, and what happens when their target class is gone. Expected: kept; delivery to a missing class is dropped. | Observe on a disposable device: `dumpsys alarm` and `dumpsys jobscheduler` before and after the update. |
| U7 | Whether any 1.2.7-era install still has widgets bound to other provider class names besides `widget.TasksWidget`. | Check the 1.2.7 tag's generated manifest; list bound widgets with `dumpsys appwidget` on an old install. |
| U8 | Exact on-device layout of Expo's multi-user path (`/data/user/<N>/`) for work profiles, and whether stored `file://` attachment URIs ever point at another user id. | Seed on a work profile and read stored URIs. |
| U9 | The earliest RN version a user can jump from straight to native (Play and F-Droid can skip versions). The native app inherits every RN migration that version would still need (legacy JSON keys, expo-sqlite era). | Decide a minimum supported source version; add one harness row per supported source version. |
